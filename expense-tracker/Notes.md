stage_1.py: ValueError: could not convert string to float: 'abcd' - because supplied string instead of the integer or float
stage_2.py: lopping on empty list returns nothing as it doesn't have any values
stage_3.py: if __name__ == "__main__": executes the function assigned to it in the active file. if we are running current file's functions from a different python file (where we imported current file's methods), the block if __name__ doesn't get executed
stage_4.py: haivng unstructural things in json will throw "json.decoder.JSONDecodeError"
stage_4.py: JSON file look like [{'key1': value1},{'key2': value2}]
stage_4.py: json.dump() is used to insert into existing JSON, where in json.load() is used to override the json content